class Solution {
public:
    int minBitFlips(int s, int g) {
        int c=0;
        while(s!=0 || g!=0){
            int i=s&1;
            int j=g&1;
            if(i!=j) c++;
            s>>=1;
            g>>=1;
        }
        return c;
    }
};
