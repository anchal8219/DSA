class Solution {
public:
    vector<int> decode(vector<int>& e, int f) {
        vector<int> res;
        res.push_back(first);
        for(int i = 0; i < encoded.size(); i++)
            res.push_back(res[i] ^ encoded[i]);
        return res;
    }
};# DSA
