Node* pairWiseSwap(struct Node* head) 
    {
        if(!head ||!head->next)
            return head;
        Node* newHead=head->next;
        Node* temp=newHead->next;
        newHead->next=head;
        newHead->next->next=pairWiseSwap(temp);
        return newHead;
        
    }
