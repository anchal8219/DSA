class Solution {
public:
    bool hasTrailingZeros(vector<int>& nums) {
        int n=nums.size();
        int c=0;
        for(int i=0;i<n;i++){
            if((nums[i]&1)==1) c++;
                
        }
        
        if((n-c)>=2) return true;
        else return false;
    }
};
