int hammingDistance(int x, int y) {
        int c=0;
        
            int r = x^y;
            while(r!=0) {
                if((r&1)==1)
                c++;
                r = r>>1;
            }
        return c;
    }
