 vector<long long> printFibb(int n) 
    {
        long long a=0,b=1;
        vector<long long>res={1};
        while(--n)
        {
            long long c=a+b;
            a=b;
            b=c;
            res.push_back(c);
        }
        return res;
    }
