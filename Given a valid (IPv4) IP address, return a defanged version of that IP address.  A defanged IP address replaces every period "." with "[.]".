# DSA
class Solution {
public:
    string defangIPaddr(string add) {
        // vector<char>v;
        string s="";
        for(int i=0;i<add.length();i++){
            if(add[i]=='.'){
                s+="[.]";
            }
            else s+=add[i];
        }
        return s;
    }
};
