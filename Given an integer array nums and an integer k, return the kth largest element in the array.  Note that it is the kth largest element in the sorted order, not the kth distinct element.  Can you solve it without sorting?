class Solution {
public:
    int findKthLargest(vector<int>& nums, int k) {
        priority_queue<int>p(nums.begin(),nums.end());
        int f;
        while(k--){
            f= p.top();
            p.pop();

        }
        return f;
    }
};
