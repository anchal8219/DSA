int main() {
	// your code goes here
	int t,x,y,min;
	cin>>t;
	while(t--){

	    cin>>x>>y;
	    min=5*x;
	    int g= ((2*y)+(1*x));
	    if(g<min )
	    min = g;
	    int h= ((1*y)+(3*x));
	    
	    if(h<min && h<g)
	    min = h;
	    
	   // while()
	    cout<<min<<endl;
	    
	}
	return 0;
}
