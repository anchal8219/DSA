int maximizeSum(vector<int>& nums, int k) {
        sort(nums.begin(),nums.end());
        int g = nums.back();
        int sum=0;
        for(int i=0;i<k;i++){
            sum+= g;
            g = g+1;
        }
        
        return sum;
    }
