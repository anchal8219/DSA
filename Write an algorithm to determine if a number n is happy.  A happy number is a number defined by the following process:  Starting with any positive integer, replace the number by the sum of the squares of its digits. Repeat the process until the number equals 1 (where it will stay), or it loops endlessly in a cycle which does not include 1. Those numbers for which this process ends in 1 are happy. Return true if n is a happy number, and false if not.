bool isHappy(int n) {
        int sum=0;
        if(n==1)
        return true;
        if(n<5 && n!=1)
        return false;
            while(n>0){
                int r=n%10;
                sum=sum+(r*r);
                n=n/10;
            }
            
            return isHappy(sum);
    }
