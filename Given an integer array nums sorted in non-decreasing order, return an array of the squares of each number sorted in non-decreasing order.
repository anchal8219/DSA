class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int>n;
        int j;
        for(int i=0;i<nums.size();i++){
            j=nums[i]*nums[i];
            n.push_back(j);
        }
        
        sort(n.begin(),n.end());
        return n;
    }
    
};
