priority_queue<pair<int,int>>p;
        unordered_map<int,int>m;
        for(auto x: nums) m[x]++;

        for(auto i:m){
            // m[nums[i]]++;
            p.push({i.second,i.first});

        }
        vector<int>v;
        while(k--){
            v.push_back(p.top().second);
            p.pop();
        }
    return v;
