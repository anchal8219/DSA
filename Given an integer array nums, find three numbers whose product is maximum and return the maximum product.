class Solution {
public:
    int maximumProduct(vector<int>& nums) {
        int n=nums.size();
        sort(nums.begin(),nums.end());
        int p=1,q=1;
        p = nums[0]*nums[1]*nums[n-1];
        q = nums[n-1]*nums[n-2]*nums[n-3];
        int res = max(p,q);
        return res;

    }
};
