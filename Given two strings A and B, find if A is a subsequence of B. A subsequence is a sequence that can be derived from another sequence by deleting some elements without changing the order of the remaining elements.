class Solution
{
    public:
    bool isSubSequence(string A, string B)
    {
        int j=0;
        int m=A.length(),n=B.length();
        for(int i=0;i<n && j<m;i++){
            if(B[i]==A[j])
            j++;
        }
        return (j==m);
    }
};
