class Solution {
public:
    bool canJump(vector<int>& nums) {
        int n=nums.size(),mn=0;
        for(int i=n-2;i>=0;i--){
            mn++;
            if(nums[i]>=mn) mn=0;
        }
        if(mn==0) return true;
        return false;
    }
};
