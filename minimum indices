int countIndices(int n, vector<int> &arr) {
        
        int c=0;
        vector<int> b(arr);
        sort(b.begin(),b.end());

        for(int i=0;i<n;i++){
            if(arr[i]!=b[i]) c++;
        }
        return c;
        
    }
