class Solution {
public:
    string restoreString(string s, vector<int>& ind) {
        map<int,char>mp;
        for(int i=0;i<s.length();i++){
            mp[ind[i]]=s[i];
        }
        string ans="";
        for(auto j:mp){
            ans+= j.second;
        }
        return ans;
    }
};
