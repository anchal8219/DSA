class Solution {
public:
    int bitwiseComplement(int num) {
       int mask = 1;
        
        while((mask & n) != n){
            mask = mask << 1;
            mask += 1;
        }
        return mask ^ n;
    }
};# DSA
